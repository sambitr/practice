# practice

### How to findout code where test coverage is missing:

```
pytest .\practice\ --cov=src --cov-report=term-missing
```

** change the directory name as per your project directory structure.